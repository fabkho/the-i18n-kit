---
title: 'move'
description: ''
seo:
  description: 'Move a translation key to another layer, another key path, or both — carrying every locale that defines it'
---

<!-- Generated by docs/generate. Do not edit by hand: change the source definition or the generator, then run `pnpm docs:generate`. -->

Move a translation key to another layer, another key path, or both — carrying every locale that defines it

## Synopsis

```bash
the-i18n-cli move --layer <layer> --key <key>
```

## Flags

| Flag | Type | Required | Default | Description |
| --- | --- | --- | --- | --- |
| `--layer` | `string` | yes | — | Layer the key lives in today |
| `--key` | `string` | yes | — | Dot-separated key path to move |
| `--toLayer` | `string` | no | — | Layer to move it to (see layerGraph.shared in the discover output for where a shared key belongs). Omit to rename within the same layer |
| `--newKey` | `string` | no | — | Key path to give it. Omit to keep the current path |
| `--dryRun` | `boolean` | no | `false` | Preview changes without writing |

Every command accepts the [shared flags](/reference/cli#shared-flags) and sets one of the [exit codes](/reference/cli#exit-codes) documented on the overview.
