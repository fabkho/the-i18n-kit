---
title: 'translate_key'
description: ''
seo:
  description: 'Add or update one source translation key and translate it into the target locales. Unlike translate_missing, this can overwrite an existing but stale target translation. Same two modes as translate_missing: provider mode (the server env-configured) translates directly; agent mode returns a fallbackContext — translate it inline and persist via write_translations.'
---

<!-- Generated by docs/generate. Do not edit by hand: change the source definition or the generator, then run `pnpm docs:generate`. -->

Add or update one source translation key and translate it into the target locales. Unlike translate_missing, this can overwrite an existing but stale target translation. Same two modes as translate_missing: provider mode (the server env-configured) translates directly; agent mode returns a fallbackContext — translate it inline and persist via write_translations.

## Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- | --- |
| `layer` | `string` | yes | Layer holding the key, from discover (e.g., "root", "app-admin"). |
| `key` | `string` | yes | Dot-separated key path to translate. Example: "bookingCreator.options.removeSubResource". |
| `sourceLocale` | `string` | yes | Source locale ref. May be a code ("en-us"), a language ("en-US") or a file ("en-US.json"). |
| `sourceValue` | `string` | no | Source value to write before translating. If omitted, the existing source value is read. |
| `targetLocales` | `"all" \| string[]` | no | Locales to translate into. Pass "all", or omit, for every locale except the source. |
| `overwrite` | `boolean` | no | Overwrite existing target translations. When false, only missing targets are filled. Default: true. |
| `dryRun` | `boolean` | no | Report the source and target locales without writing files or calling the translation backend. Default: false. |
| `includePreview` | `boolean` | no | Include the translated values in the result. Default: false, which keeps the response compact. |
| `projectDir` | `string` | no | Absolute path to the project root. Defaults to I18N_PROJECT_DIR, then server cwd. Example: "/home/user/my-app". |

## Behavior Hints

A host reads these to decide whether a call needs your confirmation first.

| Hint | Value |
| --- | --- |
| `readOnlyHint` | `false` |

## Paired CLI Command

The same operation runs from a terminal as [`the-i18n-cli translate-key`](/reference/cli/translate-key), whose page documents its flags. Both paths call the same `@the-i18n-kit/cli` functions, so the result is the same.

Every tool is advertised by `the-i18n-mcp` and listed on the [MCP tools overview](/reference/mcp).
