{
  "name": "@the-i18n-kit/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "generate:reference": "tsx generate/index.ts",
    "preview": "nuxt preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.generate.json"
  },
  "dependencies": {
    "docus": "^5.12.3",
    "nuxt": "^4.4.8"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.20.6",
    "vitest": "^3.2.0",
    "yaml": "2.8.2"
  }
}
